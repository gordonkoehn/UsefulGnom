name: usefulgnom
# Conda/Mamba environment file,
# to be used when running the snakemake pipeline, 
# see workflow/README.md
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=^3.12
  - snakemake=8.19.3
  - poetry=1.8.3